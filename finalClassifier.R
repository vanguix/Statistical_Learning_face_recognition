finalClassifier <- function(name){
rm(list=ls())
## libraries

#read and transform the test image
current_path <- getwd()
image_directory <- current_path + name
  
# Read all the images
test_image = ##leer solo una imagen

#test_matrix= transform()
#load the trained model

#load("mydata.RData")

#call the function

#prediction= our_knn_single(train_fisher, test_fisher, target, friends=k, threshold= thres, metric=metric)

return(files_list)}


#Call the function
name="anonym1.4.jpg"  #name of the image
prediction= finalClassifier(name)